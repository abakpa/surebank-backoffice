name: surebank-backoffice
static_sites:
  - name: backoffice
    source_dir: /
    build_command: npm run build
    output_dir: build
    index_document: index.html
    catchall_document: index.html
    routes:
      - path: /
